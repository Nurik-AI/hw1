 
let con = confirm(' вам есть 18?')

if (con > false) {
    let prom = prompt ('введите имя')
    if (prom) {
        alert( 'добро пожаловать!' + prom)
    } else 
        ('вход запрещен!')
    
        }else {
        alert('вход запрещен!')
    }
